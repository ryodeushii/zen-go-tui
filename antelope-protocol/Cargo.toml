[package]
name = "antelope-protocol"
version = "0.1.0"
edition = "2021"
description = "Protocol definitions and encoding/decoding for Antelope Audio Zen Go Synergy Core"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
