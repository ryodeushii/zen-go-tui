export struct OutputView {
    index: int,
    name: string,
    level-step: int,
    level-db: int,
    level-percent: int,
    muted: bool,
    dimmed: bool,
}

export struct MixerStripView {
    channel: int,
    name: string,
    assignment: string,
    level-step: int,
    level-db: int,
    meter-percent: int,
    pan-raw: int,
    pan: int,
    muted: bool,
    soloed: bool,
    linked: bool,
    linkable: bool,
    selected: bool,
}

export struct PreampView {
    input: int,
    name: string,
    mode: string,
    gain: int,
    meter-percent: int,
    phantom: bool,
    phase-inverted: bool,
    selected: bool,
}

export struct ChoiceView {
    index: int,
    label: string,
    selected: bool,
}
